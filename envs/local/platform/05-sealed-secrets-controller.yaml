###############################################################################
# Sealed Secrets controller — encrypted-at-rest secrets in Git.
#
# Naming alignment: the chart defaults to release name `sealed-secrets`, but
# the kubeseal CLI defaults to looking up `sealed-secrets-controller`. We
# override fullnameOverride to bridge the gap so kubeseal works without flags.
###############################################################################
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: sealed-secrets-controller
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "-49"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  labels:
    app.kubernetes.io/part-of: zylos-platform
    app.kubernetes.io/component: secrets-controller
    zylos.app/environment: local
spec:
  project: platform
  sources:
    - repoURL: https://bitnami.github.io/sealed-secrets
      chart: sealed-secrets
      targetRevision: 2.18.5
      helm:
        releaseName: sealed-secrets
        valueFiles:
          - $values/helm-values/sealed-secrets-controller/values.yaml
          - $values/helm-values/sealed-secrets-controller/values-local.yaml

    - repoURL: https://github.com/zylos-platform/zylos-infra-gitops.git
      targetRevision: main
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: sealed-secrets
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - ServerSideApply=true
      - CreateNamespace=false
    retry:
      limit: 5
      backoff:
        duration: 30s
        factor: 2
        maxDuration: 5m

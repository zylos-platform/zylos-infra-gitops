###############################################################################
# zylos-service-cart Argo CD Application.
###############################################################################
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: zylos-service-cart
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "20" # After all platform components
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  labels:
    app.kubernetes.io/part-of: zylos-platform
    app.kubernetes.io/component: application-service
    zylos.app/environment: local
spec:
  project: apps
  sources:
    - repoURL: https://github.com/zylos-platform/zylos-helm-charts.git
      targetRevision: main
      path: zylos-service-base
      helm:
        releaseName: zylos-service-cart
        valueFiles:
          - $values/helm-values/services/zylos-service-cart/values.yaml
          - $values/helm-values/services/zylos-service-cart/values-local.yaml

    - repoURL: https://valkey.io/valkey-helm
      chart: valkey
      targetRevision: 0.11.0
      helm:
        releaseName: cart-valkey
        valueFiles:
          - $values/helm-values/valkey/values.yaml
          - $values/helm-values/services/zylos-service-cart/valkey-values.yaml

    - repoURL: https://github.com/zylos-platform/zylos-infra-gitops.git
      targetRevision: main
      ref: values

    - repoURL: https://github.com/zylos-platform/zylos-infra-gitops.git
      targetRevision: main
      path: components/services/zylos-service-cart/overlays/local
  destination:
    server: https://kubernetes.default.svc
    namespace: zylos-cart
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=false
      - ServerSideApply=true
    retry:
      limit: 5
      backoff:
        duration: 30s
        factor: 2
        maxDuration: 5m

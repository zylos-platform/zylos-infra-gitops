auth:
  enabled: false

replica:
  enabled: false        # standalone single instance

# Cache semantics: cap memory, evict least-recently-used rather than OOM.
valkeyConfig: |
  maxmemory 256mb
  maxmemory-policy allkeys-lru

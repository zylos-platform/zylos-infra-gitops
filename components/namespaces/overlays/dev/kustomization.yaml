apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
patches:
  - target:
      kind: Namespace
      labelSelector: zylos.app/mesh-eligible=true
    patch: |
      - op: add
        path: /metadata/labels/istio.io~1dataplane-mode
        value: ambient

###############################################################################
# zylos-service-hello Argo CD Application.
#
# Composes the shared chart helm/zylos-service-base/ with the per-service
# values file helm-values/services/zylos-service-hello.yaml. Both come from
# this same repo (single source).
###############################################################################
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: zylos-service-hello
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "20" # After all platform components
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  labels:
    app.kubernetes.io/part-of: zylos-platform
    app.kubernetes.io/component: application-service
spec:
  project: apps
  sources:
    - repoURL: https://github.com/zylos-platform/zylos-infra-gitops.git
      targetRevision: main
      path: helm/zylos-service-base
      helm:
        releaseName: zylos-service-hello
        valueFiles:
          - $values/helm-values/services/zylos-service-hello.yaml
    - repoURL: https://github.com/zylos-platform/zylos-infra-gitops.git
      targetRevision: main
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: zylos-services
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
    retry:
      limit: 5
      backoff:
        duration: 30s
        factor: 2
        maxDuration: 5m
